# 实验操作指南

本指南涵盖所有 33 张报告图片的获取方法，包括 ModelSim 仿真、Quartus 综合、截图及报告排版全流程。

---

## 零、准备工作

### 0.1 文件对照

| 报告图号 | 类型 | 来源 |
|---------|------|------|
| 图1 | 参考时序图（从官方文档截取） | 竞赛试题 PDF 文档 |
| 图2~图6 | 设计框图（Visio/draw.io 绘制） | 自己画 |
| 图4 | RTL 视图 | Quartus RTL Viewer |
| 图7, 图24 | 芯片封装图 | Quartus Pin Planner |
| 图8~图14 | 题目一仿真波形 | ModelSim |
| 图15~图17 | 题目一综合结果 | Quartus Compilation Report |
| 图18~图23 | 设计框图（Visio/draw.io 绘制） | 自己画 |
| 图25~图31 | 题目三仿真波形 | ModelSim |
| 图32~图33 | 题目三综合结果 | Quartus Compilation Report |

---

## 一、题目一 ModelSim 仿真（获取图8~图14，共 7 张）

### 1.1 启动仿真

1. 从开始菜单打开 ModelSim
2. 在 Transcript 窗口底部命令栏输入：

```
cd C:/Users/zhang/Desktop/FPGA/my_new/problem1/sim
do run_sim.do
```

3. 脚本自动编译 `testbench.v`（官方）、`network_parser.v`（DUT）和 `top_sim.v`（wrapper），以 `top_sim` 为顶层运行 3000 ns 仿真。
4. 仿真结束后 Wave 窗口会弹出。若没有弹出，点击 View → Wave。

### 1.2 波形窗口布局

脚本已预设 4 个信号分组：

- **AXI-Stream** 组：clk, rst_n, tvalid, tdata, tkeep, tready, tlast
- **Parser Outputs** 组：word_cnt（需手动添加）, is_ipv4（需手动添加）, fourth_protocl, SrcIP, DesIP, ICMP_Type, data_valid
- **Storage** 组：WEA, DINA, ADDRA, frame_idx（需手动添加）
- **Frame Info** 组：dst_ip_low_temp（需手动添加）

### 1.2.5 信号含义速查

波形中每个信号的含义、位宽和怎么看：

| 信号名 | 位宽 | 含义 | 怎么看 |
|--------|------|------|--------|
| clk | 1 | 系统时钟，156.25 MHz | 方波，周期 6.4 ns。所有信号在 clk 上升沿变化 |
| rst_n | 1 | 异步复位，**低电平有效** | 为 0 时系统处于复位状态；为 1 时正常工作。波形开头 rst_n=0，约 50ns 后变 1 |
| tvalid | 1 | AXI-Stream 主端数据有效 | 为 1 表示当前 tdata 上的数据有效。每帧传输期间（100 个 Word）为 1，帧间空隙为 0 |
| tdata | 64 | AXI-Stream 64 位数据总线 | 十六进制显示。一个时钟周期传 8 字节。跨 Word 边界的字段需要拼接 |
| tkeep | 8 | 字节有效指示 | 8'hFF 表示 tdata 全部 8 字节有效。通常始终为 0xFF |
| tready | 1 | AXI-Stream 从端就绪 | 由 DUT 输出。复位期间为 0，复位释放后常为 1（= rst_n） |
| tlast | 1 | 帧结束标志 | 每帧最后一个 Word 时拉高 1 拍。tlast=1 后，下一拍 word_cnt 归零 |
| word_cnt | 8 | 帧内 Word 序号计数器 | 0→1→2→...→99→(tlast)→0。只在 case 1/2/3/4 时触发解析，≥5 后无操作 |
| is_ipv4 | 1 | IPv4 协议识别标志 | 内部信号需手动添加。word_cnt=1 时检测 tdata[47:32]==0x0800。1=IPv4，0=非 IPv4 |
| fourth_protocl | 8 | 四层协议号（输出） | word_cnt=2 时从 tdata[63:56] 提取。IPv4 时为 0x01（ICMP）；非 IPv4 时为 0xFF |
| SrcIP | 32 | 源 IP 地址（输出） | word_cnt=3 时从 tdata[47:16] 提取。如 0xC801080a = 200.1.8.10 |
| DesIP | 32 | 目的 IP 地址（输出） | word_cnt=3 时锁存低 16 位 + word_cnt=4 时拼接高 16 位。**跨 Word 拼接字段，验证重点** |
| ICMP_Type | 8 | ICMP 类型（输出） | word_cnt=4 时从 tdata[23:16] 提取。非 IPv4 时为 0xFF |
| data_valid | 1 | 解析完成脉冲（输出） | word_cnt=4 时拉高 **1 个时钟周期**。告诉 store_ctrl "当前帧解析完了，可以写存储" |
| dst_ip_low_temp | 16 | DesIP 低 16 位暂存器 | 内部信号需手动添加。word_cnt=3 时锁存 tdata[63:48]，word_cnt=4 时拼接到 DesIP 高位 |
| WEA | 1 | 存储器写使能 | 两拍写入期间为 1，其余时间为 0。每帧连续两拍：写偶数地址→写奇数地址 |
| DINA | 64 | 存储器写数据总线 | beat=0：{DesIP, SrcIP}；beat=1：{48'd0, ICMP_Type, fourth_protocl} |
| ADDRA | 3 | 存储器写地址 | 0x0~0x5 循环。{frame_idx, beat} 拼接生成。帧 0 占 0x0/0x1，帧 1 占 0x2/0x3，帧 2 占 0x4/0x5 |
| beat | 1 | 两拍写入状态机 | 内部信号需手动添加。0=写偶数地址，1=写奇数地址。data_valid 后 0→1→0 |
| frame_idx | 2 | 帧序号计数器 | 内部信号需手动添加。0→1→2→0 循环。每帧处理完后自增 |

---

### 1.3 图8 --- 第一帧 axis_rx_parser 功能仿真波形图

**报告位置：** 第五章 "各模块仿真、波形图及结果分析" → 第一帧报文 → axis_rx_parser 小节

**这张图要展示什么：**

VLAN 帧（非 IPv4，EtherType=0x0081）被正确识别为非法帧，所有特征字段输出全部置为全 1（0xFF）。

**操作步骤：**

1. 在 Wave 窗口中点击 **Zoom Full** 图标使全部波形可见
2. 找到第一帧区域（约 50 ns ~ 700 ns），在波形上拖拽鼠标框选该区间，点击工具栏 **Zoom In** 放大
3. 在左侧信号面板中调整可见信号。保留以下信号，其余可右键 → Delete 删除：

   | 信号名 | 添加命令（如未自动显示） |
   |--------|------------------------|
   | clk | 自动 |
   | rst_n | 自动 |
   | tvalid | 自动 |
   | tdata | 自动 |
   | word_cnt | `add wave /top_sim/u_dut/u_parser/word_cnt` |
   | is_ipv4 | `add wave /top_sim/u_dut/u_parser/is_ipv4` |
   | fourth_protocl | 自动 |
   | SrcIP | 自动 |
   | DesIP | 自动 |
   | ICMP_Type | 自动 |
   | data_valid | 自动 |

4. 调整信号顺序（拖拽信号名即可重排），建议自上而下：
   clk → tvalid → tdata → word_cnt → is_ipv4 → fourth_protocl → SrcIP → DesIP → ICMP_Type → data_valid

5. **验证点——截图前逐一核对：**
   - word_cnt 每帧从 0 开始递增（经过 1/2/3/4 时 case 触发解析），继续增至 99，tlast 拉高后归 0
   - is_ipv4 全程为 **0**（低电平），说明 EtherType != 0x0800
   - fourth_protocl 在 word_cnt=4 之后稳定输出 **0xFF**
   - SrcIP 在 word_cnt=4 之后稳定输出 **0xFFFFFFFF**
   - DesIP 在 word_cnt=4 之后稳定输出 **0xFFFFFFFF**
   - ICMP_Type 在 word_cnt=4 之后稳定输出 **0xFF**
   - data_valid 在 word_cnt=4 时刻产生 **一个单周期高电平脉冲**

6. 将黄色 cursor 线拖到 data_valid 的上升沿，确认此时 word_cnt=4、is_ipv4=0
7. 右键信号名 → Radix → Hexadecimal（确保数值以十六进制显示）
8. 按 **Win + Shift + S**，框选 Wave 窗口信号区域，截图
9. 粘贴到报告 "图8" 占位符处（搜索 "图8" 或第 8 个 "此处插入图片"）

**图片中应清晰可见的信号值：**
- word_cnt: 0→1→2→3→4→5→...→99→(tlast)→0。每帧 100 个 Word，case 只在 1~4 时工作，>=5 后不触发任何操作
- is_ipv4: 0
- fourth_protocl: 0xFF
- SrcIP: 0xFFFFFFFF
- DesIP: 0xFFFFFFFF
- ICMP_Type: 0xFF
- data_valid: 一个明显的高脉冲

---

### 1.4 图9 --- 第一帧 store_ctrl 功能仿真波形图

**报告位置：** 第五章 → 第一帧报文 → 数据写模块 store_ctrl 小节

**这张图要展示什么：**

第一帧（VLAN 非 IPv4）经 store_ctrl 写入存储器的两拍过程：第一拍写偶数地址、第二拍写奇数地址，WEA 在两拍后归零。

**操作步骤：**

1. 保持同一波形窗口，删除 Parser 相关信号，保留/添加以下信号：

   | 信号名 | 添加命令 |
   |--------|---------|
   | clk | 自动 |
   | data_valid | 自动 |
   | WEA | 自动 |
   | DINA | 自动 |
   | ADDRA | 自动 |
   | beat | `add wave /top_sim/u_dut/u_store/beat` |
   | frame_idx | `add wave /top_sim/u_dut/u_store/frame_idx` |

2. Zoom 到第一帧 data_valid 脉冲附近（约 word_cnt=4 之后 3~5 个周期）
3. **验证点：**
   - data_valid 脉冲后，第一拍: **WEA=1**, **ADDRA=0x0**, **DINA=0xFFFFFFFF_FFFFFFFF**
   - 第二拍: **WEA=1**, **ADDRA=0x1**, **DINA=0x000000000000FFFF**
   - 两拍结束后 beat 归 0，**WEA=0**，frame_idx 从 0 变为 **1**
4. 用 cursor 分别放在第一拍和第二拍的 WEA 上升沿，确认 ADDRA 和 DINA 值
5. 截图，粘贴到 "图9"

---

### 1.5 图10 --- 第一帧 data_storage 功能仿真波形图

**报告位置：** 第五章 → 第一帧报文 → 数据存储模块 data_storage 小节

**这张图要展示什么：**

寄存器堆 mem 数组在帧 1 写入后的内容：mem[0] 和 mem[1] 已更新，其余地址为 0。

**操作步骤：**

1. 在 Transcript 中输入：
   ```
   add wave -radix hex /top_sim/u_dut/u_storage/mem
   ```
2. 信号面板出现 mem[0] ~ mem[7] 共 8 个信号
3. Zoom 到第一帧写入完成后的区间
4. **验证点：**
   - **mem[0] = 64'hFFFFFFFF_FFFFFFFF** ✓
   - **mem[1] = 64'h000000000000FFFF** ✓
   - mem[2] ~ mem[7] = 64'h0000000000000000（未写入，保持 0）
5. 截图，粘贴到 "图10"

---

### 1.6 图11 --- 第二帧 axis_rx_parser 功能仿真波形图

**报告位置：** 第五章 → 第二帧报文（IPv4-ICMP 测试 A）→ axis_rx_parser 小节

**这张图要展示什么：**

第二帧（IPv4/ICMP，IP_ICMP_2）被正确识别并解析，is_ipv4=1，提取的字段值正确。

**操作步骤：**

1. 与图8同样的信号布局（clk, tvalid, tdata, word_cnt, is_ipv4, fourth_protocl, SrcIP, DesIP, ICMP_Type, data_valid）
2. Zoom 到第二帧区域（约 700 ns ~ 1350 ns）
3. **验证点：**
   - **is_ipv4 = 1**（高电平）—— 与图8 的关键区别
   - **fourth_protocl = 0x01**（ICMP 协议号）
   - **SrcIP = 0xC801080a**（即 200.1.8.10）
   - **DesIP = 0x6401080a**（即 100.1.8.10）—— 注意跨 word_cnt=3 和 word_cnt=4 拼接
   - **ICMP_Type = 0x11**
   - **data_valid** 在 word_cnt=4 时刻单周期高脉冲
4. 用 cursor 在 word_cnt=3 切换观察，注意 dst_ip_low_temp 的值在 word_cnt=3 时锁存、word_cnt=4 时拼接
5. 截图，粘贴到 "图11"

---

### 1.7 图12 --- 第二帧 store_ctrl 功能仿真波形图

**报告位置：** 第五章 → 第二帧报文 → store_ctrl 小节

**操作步骤：**

1. 与图9同样的信号布局，Zoom 到第二帧 data_valid 脉冲后的两拍
2. **验证点：**
   - 第一拍: WEA=1，**ADDRA=0x2**，**DINA=0x6401080a_C801080a**
   - 第二拍: WEA=1，**ADDRA=0x3**，**DINA=0x0000000000001101**
   - frame_idx 从 1 变为 **2**
3. 截图，粘贴到 "图12"

---

### 1.8 图13 --- 第三帧 axis_rx_parser 功能仿真波形图

**报告位置：** 第五章 → 第三帧报文（IPv4-ICMP 测试 B）→ axis_rx_parser 小节

**操作步骤：**

1. 与图8同样的信号布局，Zoom 到第三帧区域（约 1350 ns ~ 2000 ns）
2. **验证点（注意与图11的 SrcIP/DesIP 互换）：**
   - **is_ipv4 = 1**
   - **SrcIP = 0x6401080a**（100.1.8.10）—— 与图11互换
   - **DesIP = 0xC801080a**（200.1.8.10）—— 与图11互换
   - **ICMP_Type = 0x22** —— 与图11不同
   - **fourth_protocl = 0x01**
   - **data_valid** 单周期脉冲
3. 截图，粘贴到 "图13"

---

### 1.9 图14 --- 第三帧 store_ctrl 功能仿真波形图

**报告位置：** 第五章 → 第三帧报文 → store_ctrl 小节

**操作步骤：**

1. 与图9同样的信号布局，Zoom 到第三帧 data_valid 后的两拍
2. **验证点：**
   - 第一拍: WEA=1，**ADDRA=0x4**，**DINA=0xC801080a_6401080a**
   - 第二拍: WEA=1，**ADDRA=0x5**，**DINA=0x0000000000002201**
   - frame_idx 从 2 变为 **0**（回绕，因为只有 3 帧）
3. 截图，粘贴到 "图14"

---

## 二、题目一 Quartus 综合（获取图4, 图7, 图15~图17，共 5 张）

### 2.1 创建 Quartus 项目

1. 打开 Quartus Prime 24.1std.0
2. File → New Project Wizard
3. 点击 Next，设置：
   - 工作目录：`C:\Users\zhang\Desktop\FPGA\my_new\problem1`
   - 项目名：`network_parser`
   - 顶层模块：`network_parser`
4. 点击 Next，添加文件：`rtl/network_parser.v`（**只加这个文件**，不要加 testbench.v 和 top_sim.v）
5. 点击 Next，器件选择：
   - Family: Cyclone IV E
   - Device: EP4CE55F23C8
6. 点击 Next → Finish

### 2.2 添加时序约束

1. Assignments → Settings → TimeQuest Timing Analyzer
2. 在 SDC files to include 中点击 "..." 浏览添加：`constraints.sdc`
3. 点击 OK

### 2.3 全编译

1. Processing → Start Compilation（或 Ctrl+L）
2. 等待编译完成（约 2-5 分钟），左下角显示 "Full Compilation was successful"

---

### 2.4 图7 --- 芯片封装图

**报告位置：** 第四章 "模块设计框图、引脚说明、相关时序" → 引脚说明小节

**这张图要展示什么：**

Cyclone IV E EP4CE55F23C8 芯片的顶层封装视图，确认所用 FPGA 型号和引脚布局。

**操作步骤：**

1. 编译完成后，Tools → Chip Planner
2. 在 Chip Planner 中调整缩放使整个芯片可见
3. 截图，粘贴到 "图7"

---

### 2.5 图4 --- axis_rx_parser 模块 RTL 视图

**报告位置：** 第四章 → 模块设计框图 → axis_rx_parser 小节

**这张图要展示什么：**

Quartus 综合后自动生成的 RTL 级电路图，展示 axis_rx_parser 内部逻辑元件（LUT、寄存器、比较器、MUX）及其连接关系。

**操作步骤：**

1. Tools → Netlist Viewers → RTL Viewer
2. 在左侧层级树中展开 network_parser → 找到 `u_parser`（即 axis_rx_parser 实例）
3. 点击 `u_parser`，右侧显示该模块的 RTL 电路图
4. 调整视图使整个模块可见（可看到 word_cnt 寄存器、EtherType 比较器、dst_ip_low_temp 暂存器、输出选择器等逻辑块）
5. 截图，粘贴到 "图4"

---

### 2.6 图15 --- 系统编译综合结果（Flow Summary）

**报告位置：** 第六章 "系统综合及实现结果"

**这张图要展示什么：**

编译完成后的 Flow Summary 页面，显示资源使用量和器件信息。

**操作步骤：**

1. Processing → Compilation Report
2. 左侧选择 Flow Summary（默认已打开）
3. 截图范围必须包括以下指标：

   | 指标 | 位置（在 Flow Summary 中） | 预期值 |
   |------|--------------------------|--------|
   | Family / Device | 顶部 | Cyclone IV E / EP4CE55F23C8 |
   | Total logic elements | 中间偏上 | < 200 / 55,856 (< 1%) |
   | Total registers | 中间偏上 | ~174 |
   | Total pins | 中间偏上 | ~226 / 325 (70%) |
   | Total memory bits | 中间偏下 | 0 |
   | Total PLLs | 中间偏下 | 0 |

4. 确保这些数字清晰可读，截图，粘贴到 "图15"

---

### 2.7 图16 --- Fmax Summary 报告

**报告位置：** 第六章 → Fmax Summary 部分

**这张图要展示什么：**

TimeQuest 时序分析的最高频率报告，证明设计能满足 156.25 MHz 要求且有充足余量。

**操作步骤：**

1. Compilation Report → TimeQuest Timing Analyzer → Slow 1200mV 85C Model → **Fmax Summary**
2. 截图范围包括：
   - **clk** 的 Fmax 值 —— 预期 **> 300 MHz**
   - Restricted Fmax 值（如有）
   - 表格中的 "Fmax" 和 "Restricted Fmax" 两列
3. 如果 Fmax 数值旁有 "(Restricted)" 标记，则同时展开 Slow 1200mV 0C Model 查看
4. 截图，粘贴到 "图16"

---

### 2.8 图17 --- 时间裕量 WNS/WHS 报告

**报告位置：** 第六章 → WNS/WHS 部分

**这张图要展示什么：**

建立/保持时间最差负裕量，证明时序完全收敛（无红色违例）。

**操作步骤：**

1. Compilation Report → TimeQuest Timing Analyzer → Slow 1200mV 85C Model
2. 或在左侧展开 **Multicorner Timing Analysis Summary**
3. 找到各 corner 的汇总行，包含以下列：
   - **Setup Summary** 中的 **WNS**（Worst Negative Slack）—— 预期 **>= 0 ns**
   - **Hold Summary** 中的 **WHS**（Worst Hold Slack）—— 预期 **>= 0 ns**
   - **若数值 >= 0（通常显示为黑色/绿色），时序收敛。若为负（红色），需优化设计**
4. 截图确保 WNS 和 WHS 数值清晰可见，粘贴到 "图17"

---

## 三、题目三 ModelSim 仿真（获取图25~图31，共 7 张）

### 3.1 启动仿真

1. 在 ModelSim Transcript 中输入：

```
cd C:/Users/zhang/Desktop/FPGA/my_new/problem3/sim
do run_sim.do
```

2. 脚本编译 `proc.v`（DUT）和 `proc_tb.v`（官方），以 `proc_tb` 为顶层，`run -all` 自动在 `$finish` 处停止。
3. Wave 窗口自动弹出。

### 3.2 波形窗口布局

脚本已预设 4 个信号分组：

- **Control** 组：Clock, Reset, Run, Fun, Rx, Ry, Done
- **Data** 组：Data, BusWires
- **FSM** 组：state（需手动添加 `add wave /proc_tb/dut/state`）
- **Registers** 组：rf[0]~rf[3], latch_a, latch_g

### 3.2.5 信号含义速查

波形中每个信号的含义、位宽和怎么看：

| 信号名 | 位宽 | 含义 | 怎么看 |
|--------|------|------|--------|
| Clock | 1 | 系统时钟，100 MHz | 方波，周期 10 ns（`#5` 半周期）。所有寄存器在 Clock 上升沿采样 |
| Reset | 1 | 异步复位，**高电平有效** | 为 1 时清零所有状态和寄存器。0~10ns 期间为 1，之后常为 0 |
| Run | 1 | 指令执行启动信号 | 外部拉高 1 拍（10ns）触发处理器开始执行。Run=1 的时钟沿，状态机从 IDLE 跳转到 FETCH |
| Done | 1 | 指令执行完成标志（寄存器输出） | 在 DONE 状态时钟沿置位。连续指令时（Run=1）当拍清零不可见；指令间有间隔时（Run=0）可见为高 1 周期 |
| Fun[1:0] | 2 | 指令功能码 | 00=LOAD（载数），01=MOVE（转存），10=ADD（加法），11=SUB（减法） |
| Rx[1:0] | 2 | 目标寄存器地址 | 指定数据写入哪个寄存器：00=R0, 01=R1, 10=R2, 11=R3 |
| Ry[1:0] | 2 | 源寄存器地址 | MOVE 指令的数据来源；ADD/SUB 的第二操作数来源 |
| Data[7:0] | 8 | 外部 8 位数据输入 | LOAD 指令时，这个值经总线写入目标寄存器。如 Data=0x33 → R0=0x33 |
| BusWires[7:0] | 8 | 内部公共总线（观测输出） | **核心观测信号**。实时显示当前谁在驱动总线。bus_sel=0→Data 值；=1→R[Rx] 值；=2→R[Ry] 值；=3→latch_g 值 |
| state[2:0] | 3 | FSM 当前状态 | 需手动添加。0=IDLE(空闲), 1=FETCH(取数), 2=EXEC1(运算), 3=EXEC2(回写), 4=DONE(完成)。**跟踪状态跳转是理解处理器时序的关键** |
| bus_sel[1:0] | 2 | 总线选择控制 | 需手动添加。0 选 Data，1 选 R[Rx]，2 选 R[Ry]，3 选 latch_g。决定 BusWires 上显示谁的值 |
| alu_sub | 1 | ALU 加减法选择 | 需手动添加。0=加法（ADD 指令），1=减法（SUB 指令）。在 FETCH 状态根据 Fun[1] 置位 |
| rf[0]~rf[3] | 各 8 | 通用寄存器 R0~R3 | 十六进制显示。LOAD/MOVE 在 FETCH 时钟沿从 BusWires 写入；ADD/SUB 在 EXEC2 时钟沿从 latch_g 写入 |
| latch_a | 8 | ALU 第一操作数暂存器 | 需手动添加。在 FETCH 状态的时钟沿锁存 BusWires 上的值。ADD/SUB 指令使用 |
| latch_g | 8 | ALU 运算结果暂存器 | 需手动添加。在 EXEC1 状态的时钟沿锁存 ALU 计算结果（latch_a ± BusWires）。等待 EXEC2 状态通过总线写回目标寄存器 |

### 3.2.6 状态机信号配合阅读指南

看波形时按以下顺序理解处理器行为：

```
1. 看 Run 上升沿 → 指令开始
2. 看 Fun 值 → 什么指令（00/01/10/11）
3. 看 state 跳转 → 当前处于哪个节拍
4. 看 bus_sel → 谁在驱动总线（BusWires 的值从哪来）
5. 看 BusWires → 总线上实时数据
6. 看 latch_a/latch_g → ALU 操作数/结果
7. 看 rf[0:3] → 寄存器何时更新
8. 看 Done → 指令是否完成
```

**LOAD/MOVE 指令（2 周期）：** ... → FETCH → DONE → ...
- 首次从 IDLE 启动：IDLE → FETCH → DONE
- 连续执行时（Run 在 DONE 期间已拉高）：DONE 直接链接到 FETCH，跳过 IDLE
- IDLE/FETCH 进入时：bus_sel 选数据源，BusWires 显示数据
- FETCH 时钟沿：将 BusWires 写入 rf[Rx]（目标寄存器），同时 latch_a 锁存总线数据（无副作用）
- DONE：Done=1。若 Run=0 则下一拍回 IDLE；若 Run=1 则直接跳转 FETCH（链接）

**ADD/SUB 指令（4 周期）：** ... → FETCH → EXEC1 → EXEC2 → DONE → ...
- 首次从 IDLE 启动：IDLE → FETCH → EXEC1 → EXEC2 → DONE
- 连续执行时：DONE 直接链接到 FETCH，跳过 IDLE
- IDLE/FETCH 进入时：bus_sel 选 R[Rx]，BusWires = 第一操作数
- FETCH 时钟沿：latch_a 锁存第一操作数，bus_sel 切到 R[Ry]
- EXEC1 时钟沿：latch_g 锁存 ALU 结果（latch_a ± BusWires）
- EXEC2 时钟沿：将 latch_g 写入 rf[Rx]（目标寄存器），bus_sel 切到 latch_g
- DONE：Done=1。若 Run=0 则下一拍回 IDLE；若 Run=1 则直接跳转 FETCH（链接）

---

### 3.3 图25 --- 系统初始化与复位仿真波形

**报告位置：** 第六章 "测试文件仿真结果" → 系统初始化与复位小节

**这张图要展示什么：**

仿真开始阶段 Reset 信号有效时，所有内部状态被清零的过程。

**操作步骤：**

1. 点击 **Zoom Full** 使全部波形可见后，鼠标框选 **0 ns ~ 30 ns** 区间，Zoom In 放大
2. 需要手动添加的内部信号：

   | 信号 | 命令 |
   |------|------|
   | state | `add wave /proc_tb/dut/state` |
   | rf[0]~rf[3] | `add wave -radix hex /proc_tb/dut/rf` |
   | latch_a | `add wave /proc_tb/dut/latch_a` |
   | latch_g | `add wave /proc_tb/dut/latch_g` |

3. 信号顺序建议：Clock → Reset → Done → state → rf[0] → rf[1] → rf[2] → rf[3] → latch_a → latch_g → BusWires
4. **验证点：**
   - Reset = 1 期间（约 0 ~ 10 ns），Done = 0，state（/proc_tb/dut/state）= 0 (IDLE)
   - 10 ns 后 Reset 变为 0，所有寄存器保持 0
   - rf[0]~rf[3]（R0~R3）全部 = **0x00**
   - latch_a（/proc_tb/dut/latch_a）= **0x00**，latch_g（/proc_tb/dut/latch_g）= **0x00**
   - BusWires = **0x00**
5. 将 cursor 放在 10 ns 处，标注 Reset 释放时刻
6. 截图，粘贴到 "图25"

---

### 3.4 图26 --- 第一步：LOAD R0, 0x33 仿真波形

**报告位置：** 第六章 → 第一步 LOAD R0, 0x33 小节

**这张图要展示什么：**

LOAD 指令（2 周期）完整执行流程：IDLE→FETCH→DONE，R0（rf[0]）从 0x00 变为 0x33。

**操作步骤：**

1. Zoom 到 **15 ns ~ 50 ns** 区间
2. 信号布局：Clock, Reset, Run, Done（以上顶层端口自动显示）, Fun, Rx, Ry, Data, BusWires, state（/proc_tb/dut/state）, rf[0]（R0）, rf[1]（R1）, rf[2]（R2）, rf[3]（R3）
3. 右键 rf[0]~rf[3] → Radix → Hexadecimal
4. **按拍验证（用 cursor 标注每个时钟沿）：**
   - **25 ns 上升沿（IDLE→FETCH）：** Run=1（20ns 拉高）, Fun=00(LOAD), Rx=00(R0), state（/proc_tb/dut/state）从 0→1，bus_sel（/proc_tb/dut/bus_sel）=0 选 Data，BusWires=**0x33**
   - **35 ns 上升沿（FETCH→DONE）：** state 从 1→4(DONE)，**rf[0]（R0）从 0x00 变为 0x33**，BusWires 数据锁存到目标寄存器
   - **Run 在 30ns 已释放**，但第二步的 Run 已在 40ns 拉高，DONE 状态在 45ns 检测到 Run=1 后直接链接至第二步的 FETCH（不经 IDLE）
5. **核心验证：rf[0]（R0）值从 0x00 变为 0x33 的时刻（35 ns 上升沿）**
6. 截图，粘贴到 "图26"

---

### 3.5 图27 --- 第二步：LOAD R1, 0x22 仿真波形

**报告位置：** 第六章 → 第二步 LOAD R1, 0x22 小节

**操作步骤：**

1. Zoom 到 **35 ns ~ 70 ns**
2. 信号布局同图26
3. **验证点：**
   - 45 ns：DONE→FETCH 链接（上条 DONE 检测到 Run=1），Fun=00, Rx=01(R1), BusWires=**0x22**
   - 55 ns：state FETCH→DONE，**rf[1]（R1）从 0x00 变为 0x22**
   - rf[0]（R0）保持 **0x33** 不变
4. 截图，粘贴到 "图27"

---

### 3.6 图28 --- 第三步：LOAD R2, 0x11 仿真波形

**报告位置：** 第六章 → 第三步 LOAD R2, 0x11 小节

**操作步骤：**

1. Zoom 到 **55 ns ~ 90 ns**
2. 信号布局同图26
3. **验证点：**
   - 65 ns：DONE→FETCH 链接（上条 DONE 检测到 Run=1），Fun=00, Rx=10(R2), BusWires=**0x11**
   - 75 ns：state FETCH→DONE，**rf[2]（R2）从 0x00 变为 0x11**
   - rf[0]（R0）=0x33, rf[1]（R1）=0x22 保持不变
4. 截图，粘贴到 "图28"

---

### 3.7 图29 --- 第四步：ADD R0, R0, R1 仿真波形  [重点图]

**报告位置：** 第六章 → 第四步 ADD R0, R0, R1 小节

**这张图要展示什么：**

ADD 指令的完整 4 周期流程（IDLE→FETCH→EXEC1→EXEC2→DONE），重点展示 latch_a（/proc_tb/dut/latch_a）锁存第一操作数、latch_g（/proc_tb/dut/latch_g）锁存 ALU 加法结果、以及 R0（rf[0]）最终更新的全过程。

**操作步骤：**

1. Zoom 到 **75 ns ~ 130 ns**
2. 信号布局（这是最重要的仿真图之一，信号要齐全）：

   | 信号 | 命令 |
   |------|------|
   | Clock | 自动 |
   | Run | 自动 |
   | Done | 自动 |
   | state | `add wave /proc_tb/dut/state` |
   | Fun | 自动 |
   | Rx | 自动 |
   | Ry | 自动 |
   | BusWires | 自动 |
   | bus_sel | `add wave /proc_tb/dut/bus_sel` |
   | alu_sub | `add wave /proc_tb/dut/alu_sub` |
   | latch_a | `add wave /proc_tb/dut/latch_a` |
   | latch_g | `add wave /proc_tb/dut/latch_g` |
   | rf[0] | `add wave -radix hex /proc_tb/dut/rf` 展开 |
   | rf[1] | 同上 |

3. **按拍验证（5 个状态逐拍检查）：**

   | 时间 | 状态 | bus_sel | BusWires | alu_sub | 关键动作 |
   |------|------|---------|----------|---------|---------|
   | 85 ns | DONE→FETCH(链接) | 1(R[Rx]=R0) | 0x33 | 0 | Run=1（80ns拉高），rf[0]（R0）第一操作数上总线 |
   | 95 ns | FETCH | 2(R[Ry]=R1) | 0x22 | 0(加法) | **latch_a 锁存 0x33** |
   | 105 ns | EXEC1 | — | 0x22 | 0 | **latch_g 锁存 0x33+0x22=0x55** |
   | 115 ns | EXEC2 | 3(latch_g) | 0x55 | 0 | **rf[0]（R0）写入 latch_g = 0x55** |
   | 125 ns | DONE→FETCH(链接) | — | — | — | **rf[0]（R0）更新为 0x55**, 链接至第五步 MOVE |

4. 用 cursor 分别标注：
   - **Cursor A**: 95 ns 处 latch_a（/proc_tb/dut/latch_a）锁存 0x33
   - **Cursor B**: 105 ns 处 latch_g（/proc_tb/dut/latch_g）锁存 0x55
   - **Cursor C**: 115 ns 处 rf[0]（R0）变为 0x55
5. 截图，粘贴到 "图29"

---

### 3.8 图30 --- 第五步：MOVE R3, R0 仿真波形

**报告位置：** 第六章 → 第五步 MOVE R3, R0 小节

**操作步骤：**

1. Zoom 到 **115 ns ~ 150 ns**
2. 信号：Clock, Run, Done, state（/proc_tb/dut/state）, Fun, Ry, BusWires, rf[0]（R0）, rf[3]（R3）
3. **验证点：**
   - 这是 **2 周期指令**（MOVE, Fun=01）
   - 125 ns (DONE→FETCH 链接)：Run=1（120ns 拉高，上条 ADD 的 DONE 期间）, bus_sel（/proc_tb/dut/bus_sel）=2 选 R[Ry]=R0，BusWires=**0x55**
   - 135 ns (FETCH→DONE)：**rf[3]（R3）从 0x00 变为 0x55**
   - rf[0]（R0）保持 0x55
4. 截图，粘贴到 "图30"

---

### 3.9 图31 --- 第六步：SUB R1, R1, R2 仿真波形  [重点图]

**报告位置：** 第六章 → 第六步 SUB R1, R1, R2 小节

**这张图要展示什么：**

SUB 指令的完整 4 周期流程（同 ADD 结构但 alu_sub=1），展示补码减法在 ALU 中的执行过程。

**操作步骤：**

1. Zoom 到 **175 ns ~ 230 ns**
2. 信号布局与图29完全相同
3. **按拍验证：**

   | 时间 | 状态 | bus_sel | BusWires | alu_sub | 关键动作 |
   |------|------|---------|----------|---------|---------|
   | 185 ns | IDLE | 1(R[Rx]=R1) | 0x22 | 0 | Run=1（180ns拉高），rf[1]（R1）第一操作数上总线 |
   | 195 ns | FETCH | 2(R[Ry]=R2) | 0x11 | **1(减法)** | **latch_a 锁存 0x22** |
   | 205 ns | EXEC1 | — | 0x11 | 1 | **latch_g 锁存 0x22-0x11=0x11** |
   | 215 ns | EXEC2 | 3(latch_g) | 0x11 | 1 | **rf[1]（R1）写入 latch_g = 0x11** |
   | 225 ns | DONE | — | — | — | **rf[1]（R1）更新为 0x11**, 状态回 IDLE |

4. 与图29的关键区别：**alu_sub（/proc_tb/dut/alu_sub）=1**（蓝色高亮标注）
5. 用 cursor 分别标注 195 ns（latch_a 锁存）、205 ns（latch_g 锁存）、215 ns（rf[1] 写回）三个关键时刻
6. **最终状态验证：**
   - rf[0]（R0）= **0x55**
   - rf[1]（R1）= **0x11**（从 0x22 减去 0x11）
   - rf[2]（R2）= **0x11**
   - rf[3]（R3）= **0x55**
7. 截图，粘贴到 "图31"

---

## 四、题目三 Quartus 综合（获取图24, 图32~图33，共 3 张）

### 4.1 创建 Quartus 项目

1. File → New Project Wizard
2. 工作目录：`C:\Users\zhang\Desktop\FPGA\my_new\problem3`
3. 项目名：`proc`，顶层模块：`proc`
4. 添加文件：`rtl/proc.v`
5. 器件：Cyclone IV E → EP4CE55F23C8
6. 添加约束：`constraints.sdc`
7. Processing → Start Compilation

### 4.2 图24 --- 芯片封装图

**报告位置：** 第五章 "模块设计框图、引脚说明、相关时序" → 引脚说明小节

操作同图7：Tools → Chip Planner → 调整可见 → 截图 → 粘贴到 "图24"。

---

### 4.3 图32 --- 系统编译综合结果（Flow Summary）

**报告位置：** 第七章 "系统综合及实现结果"

**这张图要展示什么：**

8 位处理器综合后的资源报告，证明纯逻辑实现、无 IP 核调用。

**操作步骤：**

1. Processing → Compilation Report → Flow Summary
2. 关键验证指标：

   | 指标 | 预期值 | 说明 |
   |------|--------|------|
   | Total logic elements | < 100 | 极轻量 |
   | Total registers | ~52 | R0-R3 + latch_a + latch_g + state + 控制 |
   | Total memory bits | **0** | 纯寄存器堆实现 |
   | Embedded Multiplier 9-bit | **0** | 无乘法 |
   | Total PLLs | **0** | 无 IP 核 |
   | Fmax | > 200 MHz | 远超 100 MHz 要求 |

3. 截图确保 Memory bits = 0, Multiplier = 0, PLL = 0 均可见
4. 粘贴到 "图32"

---

### 4.4 图33 --- Fmax Summary 报告

**报告位置：** 第七章 → Fmax Summary 部分

**操作步骤：**

1. Compilation Report → TimeQuest Timing Analyzer → Slow 1200mV 85C Model → **Fmax Summary**
2. **预期：Clock 的 Fmax > 200 MHz**（远超要求的 100 MHz）
3. 截图，粘贴到 "图33"

---

## 五、设计框图绘制（获取图1~图3, 图5~图6, 图18~图23，共 11 张）

以下 11 张图需要用 **Visio** 或 **draw.io** 绘制后截图。每张图下面说明了具体画什么和怎么画。

---

### 5.1 图1 --- AXI-Stream 接收通道时序图

**报告位置：** 第一章 "设计任务与要求"

**来源：** 竞赛试题 PDF 文档中的 AXI-Stream 接口时序图

**操作：**

1. 打开 2025 年北京市大学生集成电路设计大赛数字试题 PDF
2. 找到题目一中展示 AXI-Stream 的 tvalid/tready/tdata/tlast 时序关系图
3. 截图，粘贴到 "图1"

**该图应展示的内容：**
- ACLK 时钟波形
- TVALID 和 TREADY 握手关系
- TDATA 数据有效窗口
- TLAST 包尾标记

---

### 5.2 图2 --- 题目一总体设计框图

**报告位置：** 第三章 "总体设计框图与理论分析"

**用什么画：** Visio 或 draw.io

**画什么（三层结构 + 数据流）：**

```
                         ┌── network_parser ──────────────────────────┐
                         │                                             │
  AXI-Stream 输入 ───────→  ┌──────────────────┐                      │
  (tvalid, tdata,          │   axis_rx_parser  │                      │
   tkeep, tlast)           │   (数据接收与     │                      │
                           │    信息抽取模块)  │                      │
  tready ←─────────────────│                   │──→ fourth_protocl    │
                           └────────┬──────────┘──→ SrcIP             │
                                    │             ──→ DesIP            │
                                    │             ──→ ICMP_Type        │
                                    │ data_valid  ──→ data_valid       │
                                    ↓                                  │
                           ┌──────────────────┐                      │
                           │   store_ctrl     │                      │
                           │   (数据写模块)   │                      │
                           │                   │──→ WEA               │
                           │                   │──→ DINA[63:0]        │
                           │                   │──→ ADDRA[2:0]        │
                           └────────┬──────────┘                      │
                                    │                                  │
                                    ↓                                  │
                           ┌──────────────────┐                      │
                           │   data_storage   │                      │
                           │   (数据存储模块) │                      │
                           │   64位 × 8深     │                      │
                           └──────────────────┘                      │
                         └─────────────────────────────────────────────┘
```

**要点：**
- 三个子模块用独立矩形框 + 中文名称标注
- 箭头方向表示数据流方向
- 所有接口信号标注在箭头旁
- 顶层 network_parser 用虚线大框包围

---

### 5.3 图3 --- axis_rx_parser 模块设计框图

**报告位置：** 第四章 "模块设计框图、引脚说明、相关时序" → axis_rx_parser 小节

**画什么：**

- **左侧输入：** clk, rst_n, tvalid, tdata[63:0], tready, tlast
- **核心逻辑块（从左上到右下排列）：**
  - word_cnt 计数器（每帧从 0 递增，case 语句在 1/2/3/4 触发解析，继续增至帧末，tlast 归零）
  - EtherType 比较器块（提取 tdata[47:32] 与 16'h0800 比较，输出 is_ipv4）
  - dst_ip_low_temp 暂存器块（tdata[63:48] → 锁存）
  - 位拼接逻辑块（{tdata[15:0], dst_ip_low_temp} → DesIP[31:0]）
  - 选择器阵列（is_ipv4 控制：正常值 or 全 1）
  - data_valid 译码逻辑块（word_cnt==4 时输出脉冲）
- **右侧输出：** protocol[7:0], src_ip[31:0], dst_ip[31:0], icmp_type[7:0], data_valid

---

### 5.4 图5 --- store_ctrl 模块设计框图

**报告位置：** 第四章 → store_ctrl 小节

**画什么：**

- **左侧输入：** clk, rst_n, data_valid_in, protocol, src_ip, dst_ip, icmp_type
- **核心逻辑：**
  - beat 状态机（画两态循环: beat=0 ⇄ beat=1）
  - frame_idx 计数器（0→1→2→0 循环）
  - ADDRA 地址生成: {frame_idx, beat} → ADDRA[2:0]
  - DINA 拼接逻辑:
    - beat=0: {dst_ip[31:0], src_ip[31:0]} → DINA[63:0]
    - beat=1: {48'd0, icmp_type[7:0], protocol[7:0]} → DINA[63:0]
  - WEA 输出逻辑
- **右侧输出：** WEA, DINA[63:0], ADDRA[2:0]

---

### 5.5 图6 --- data_storage 模块设计框图

**报告位置：** 第四章 → data_storage 小节

**画什么：**

- **左侧输入：** clk, we, addr[2:0], din[63:0]
- **核心结构：**
  - 3-to-8 地址译码器（addr[2:0] → 8 路独热码片选）
  - 8 行 × 64 位寄存器阵列（标注 mem[0] ~ mem[7]）
  - we 写使能门控
- **每个寄存器单元标注：** D 触发器符号，时钟 clk，使能 we&decoded，数据 din，输出 q

---

### 5.6 图18 --- 总线处理器总框图

**报告位置：** 题目三第一章 "设计任务与要求"

**来源：** 竞赛试题 PDF 文档中的处理器架构图

**操作：**

1. 打开试题 PDF
2. 找到题目三的处理器总框图（展示 Data→三态→总线→寄存器→ALU 的结构）
3. 截图，粘贴到 "图18"

**该图应展示的内容：**
- 8 位数据输入 Data 经三态门到总线
- R0-R3 寄存器组与总线的双向连接
- A 寄存器（latch_a）和 G 寄存器（latch_g）与 ALU 的关系
- 控制电路对三态门和寄存器的控制信号

---

### 5.7 图19 --- 题目三总体设计框图

**报告位置：** 第三章 "总体设计框图及详细说明"

**画什么：**

```
  Data[7:0] ─┐
             │
  ┌──────────▼─────────────────────────────────────────────┐
  │                   Bus MUX (bus_sel[1:0])               │
  │  0: Data   1: R[Rx]   2: R[Ry]   3: latch_g           │
  └──────────┬─────────────────────────────────────────────┘
             │ BusWires[7:0]
    ┌────────┼────────┬──────────────┐
    ▼        ▼        ▼              ▼
  ┌──────┐ ┌──────┐ ┌──────┐  ┌──────────┐
  │  R0  │ │  R1  │ │ latch│  │  latch   │
  │      │ │      │ │  _a  │  │   _g     │
  │(8bit)│ │(8bit)│ │(8bit)│  │ (8bit)   │
  └──┬───┘ └──┬───┘ └──┬───┘  └────┬─────┘
     │        │         │           │
     │  R2,R3 │         └──→ ALU ←──┘
     │  (同)  │              + / -
     │        │               │
     └────────┴───────────────┘
              │
  ┌───────────▼───────────────────────────────────────────┐
  │           Control FSM (5-state)                       │
  │  IDLE → FETCH → EXEC1 → EXEC2 → DONE                 │
  │                                                       │
  │  Inputs:  Run, Fun[1:0], Rx[1:0], Ry[1:0]            │
  │  Outputs: bus_sel[1:0], alu_sub, Done                │
  └───────────────────────────────────────────────────────┘
                                           Done ────────→
```

**标注要点：**
- 每个功能块标注模块名称和位宽
- 箭头标注数据流向
- FSM 的状态转移路径用不同颜色或线型标注
- 区分组合逻辑和数据路径

---

### 5.8 图20 --- 总线三态输入模块设计框图

**报告位置：** 第五章 "模块设计框图、引脚说明、相关时序" → 总线三态输入模块小节

**画什么：**

- 一个 **4 选 1 多路选择器**（MUX）
- 4 个数据输入端：
  - Data[7:0]（外部输入）
  - R0~R3 输出（内部寄存器）
  - latch_g[7:0]（运算结果）
- 选择控制端：bus_sel[1:0]
- 一个输出端：BusWires[7:0]
- 标注选择编码表：

  | bus_sel | 选通数据源 |
  |---------|-----------|
  | 00 | Data[7:0] |
  | 01 | R[Rx][7:0] |
  | 10 | R[Ry][7:0] |
  | 11 | latch_g[7:0] |

---

### 5.9 图21 --- 寄存器控制模块设计框图

**报告位置：** 第五章 → 寄存器控制模块小节

**画什么：**

- 4 个并列的 8 位寄存器 R0, R1, R2, R3
- 每个寄存器有：
  - D 输入连接 BusWires[7:0]
  - Q 输出连接到总线 MUX
  - 共享时钟 Clock
- 寄存器写入由 FSM 在同一时钟块内直接控制：LOAD/MOVE 在 FETCH 时钟沿写入，ADD/SUB 在 EXEC2 时钟沿写入
- 标注：数据在对应状态节拍的 Clock 上升沿锁存

---

### 5.10 图22 --- 运算部分模块设计框图

**报告位置：** 第五章 → 运算部分模块小节

**画什么：**

```
  BusWires[7:0]
       │
       ▼
  ┌─────────┐
  │ latch_a │  ← 使能：state == FETCH
  │ (8bit)  │
  └────┬────┘
       │  A[7:0]
       ▼
  ┌─────────────────────┐
  │       ALU           │
  │  加法器/减法器      │  ← alu_sub: 0=加, 1=减
  │  补码实现           │
  │  Result = A ± Bus   │
  └─────────┬───────────┘
            │  Result[7:0]
            ▼
       ┌─────────┐
       │ latch_g │  ← 使能：state == EXEC1
       │ (8bit)  │
       └────┬────┘
            │
            ▼
      BusWires[7:0]（经 bus_sel=3 选通）
```

**标注要点：**
- latch_a 使能时机：FETCH 状态的时钟上升沿
- latch_g 使能时机：EXEC1 状态的时钟上升沿
- alu_sub = Fun[0]（00/10=加, 01/11=减  → 实际是 Fun[0] 为 1 时减法即 Fun=11 时）
- ALU 标注 "补码加法器实现 ± 运算"

---

### 5.11 图23 --- 控制电路模块设计框图

**报告位置：** 第五章 → 控制电路模块小节

**画什么：**

**核心：5 状态 FSM 状态转移图**（圆角矩形 + 箭头标注转移条件）

```
     ┌──────┐  Run=1    ┌────────┐  Fun[1]=0(LOAD/MOVE)  ┌──────┐
     │ IDLE │──────────→│ FETCH  │───────────────────────→│ DONE │
     │  0   │           │   1    │                        │  4   │
     └──┬───┘           └───┬────┘                        └──┬───┘
        ↑                  │ Fun[1]=1(ADD/SUB)              │
        │                  ▼                               │
        │             ┌────────┐                           │
        │             │ EXEC1  │                           │
        │             │   2    │                           │
        │             └───┬────┘                           │
        │                 │                                │
        │                 ▼                                │
        │             ┌────────┐                           │
        │             │ EXEC2  │───────────────────────────┘
        │             │   3    │
        │             └────────┘
        └──────────────────────────────────────────────────┘
                        Run=0 (回到 IDLE)
```

**右侧附加：每个状态下的输出信号表**

| 状态 | bus_sel | alu_sub | latch_a_en | latch_g_en | rf 写入 | Done |
|------|---------|---------|------------|------------|---------|------|
| IDLE | 按 Fun 选择 | 0 | 0 | 0 | — | 0 |
| FETCH | 保持/切换 | 按 Fun | 1 | 0 | LOAD/MOVE: rf[Rx]≤BusWires | 0 |
| EXEC1 | — | 保持 | 0 | 1 | — | 0 |
| EXEC2 | 3(latch_g) | 保持 | 0 | 0 | ADD/SUB: rf[Rx]≤latch_g | 0 |
| DONE | — | 0 | 0 | 0 | — | 1 |

**输入信号标注：** Clock, Reset, Run, Fun[1:0], Rx[1:0], Ry[1:0]
**输出信号标注：** bus_sel[1:0], alu_sub, Done

---

## 六、插入图片到报告

1. 打开 `C:\Users\zhang\Desktop\FPGA\my_new\实验报告.docx`
2. 按 **Ctrl+F** 搜索 `此处插入图片`，定位到第一个占位符
3. **选中整个灰色占位符段落，按 Delete 删除**
4. 在对应位置 **粘贴**（Ctrl+V）已截取的图片
5. 右键图片 → 大小和位置 → 宽度设为 **14 cm**（锁定纵横比）
6. 图片下方的图注（如 "图8 第一帧 axis_rx_parser 功能仿真波形图"）**保留不删**
7. 重复以上步骤完成所有 33 张图片

### 快速定位技巧

| 在 Word 中按下 | 定位到 |
|---------------|--------|
| Ctrl+F 搜索 "此处插入图片"，第 1 个结果 | 图1 |
| Ctrl+F 搜索 "此处插入图片"，第 2 个结果 | 图2 |
| ... | ... |
| Ctrl+F 搜索 "此处插入图片"，第 33 个结果 | 图33 |

---

## 七、常见问题

**Q: ModelSim 报 "Error: (vlog-13006) Could not find the package"**
A: 检查 `cd` 是否切换到了 `sim/` 目录。`run_sim.do` 使用相对路径 `../rtl/`。

**Q: 仿真波形窗口没有信号？**
A: 检查 `vsim -voptargs="+acc"` 是否包含 `+acc` 选项（已在 .do 脚本中配置）。

**Q: Quartus TimeQuest 标红（时序违例）？**
A: 确认已添加 `constraints.sdc` 文件（Assignments → Settings → TimeQuest Timing Analyzer），然后重新编译。

**Q: `node gen_report.js` 报错 "docx" not found？**
A: 先安装 docx 包：`npm install -g docx`

**Q: 如何查看/添加内部信号到波形？**
A: 在 ModelSim Transcript 中使用 `add wave` 命令。常用命令速查：

```
# 题目一
add wave /top_sim/u_dut/u_parser/word_cnt
add wave /top_sim/u_dut/u_parser/is_ipv4
add wave /top_sim/u_dut/u_store/beat
add wave /top_sim/u_dut/u_store/frame_idx
add wave -radix hex /top_sim/u_dut/u_storage/mem

# 题目三
add wave /proc_tb/dut/state
add wave -radix hex /proc_tb/dut/rf
add wave /proc_tb/dut/latch_a
add wave /proc_tb/dut/latch_g
add wave /proc_tb/dut/bus_sel
add wave /proc_tb/dut/alu_sub
```

**Q: 波形数值显示为二进制而非十六进制？**
A: 右键信号名 → Radix → Hexadecimal

**Q: 截图后图片模糊？**
A: ModelSim Wave 窗口尽可能拉大后再截图。使用 PNG 格式保存可获得最佳清晰度。

**Q: 如何批量添加信号？**
A: 在 Transcript 中用 `add wave -radix hex /top_sim/u_dut/*` 添加某模块的所有信号，再删除不需要的。

---

## 八、截图速查汇总表

| 图号 | 内容 | 来源 | 关键指标/验证点 |
|------|------|------|----------------|
| 图1 | AXI-S 时序图 | 试题 PDF | tvalid/tready 握手, tlast |
| 图2 | 题目一顶层框图 | Visio 绘制 | 3 个子模块 + 数据流 + 接口信号 |
| 图3 | axis_rx_parser 框图 | Visio 绘制 | word_cnt, is_ipv4, 拼接逻辑, 选择器 |
| 图4 | axis_rx_parser RTL | Quartus RTL Viewer | LUT/寄存器/D触发器 连线 |
| 图5 | store_ctrl 框图 | Visio 绘制 | beat 状态机, frame_idx, ADDRA 生成 |
| 图6 | data_storage 框图 | Visio 绘制 | 8×64b 阵列, 3-8 译码器, we 门控 |
| 图7 | 芯片封装图 | Quartus Chip Planner | EP4CE55F23C8 |
| 图8 | 帧1 parser 波形 | ModelSim | is_ipv4=**0**, 全F 输出, data_valid 脉冲 |
| 图9 | 帧1 store 波形 | ModelSim | ADDRA 0x0→0x1, DINA 全F, frame_idx 0→1 |
| 图10 | 帧1 storage 波形 | ModelSim | mem[0]=全F, mem[1]=0xFFFF, 其余=0 |
| 图11 | 帧2 parser 波形 | ModelSim | is_ipv4=**1**, SrcIP=0xC801080a, ICMP_Type=0x11 |
| 图12 | 帧2 store 波形 | ModelSim | ADDRA 0x2→0x3, DINA=0x6401080a_C801080a |
| 图13 | 帧3 parser 波形 | ModelSim | SrcIP/DesIP 互换, ICMP_Type=**0x22** |
| 图14 | 帧3 store 波形 | ModelSim | ADDRA 0x4→0x5, frame_idx 回绕 2→0 |
| 图15 | 题目一 Flow Summary | Quartus | LE < 200, Reg ~174, Memory=0, PLL=0 |
| 图16 | 题目一 Fmax | Quartus TimeQuest | **Fmax > 300 MHz**, 远超 156.25 MHz |
| 图17 | 题目一 WNS/WHS | Quartus TimeQuest | **WNS >= 0, WHS >= 0**, 时序收敛 |
| 图18 | 处理器总框图 | 试题 PDF | 架构概览 |
| 图19 | 题目三顶层框图 | Visio 绘制 | 4 大组件 + 5 状态 FSM |
| 图20 | 总线 MUX 框图 | Visio 绘制 | bus_sel 4 选 1 编码表 |
| 图21 | 寄存器控制框图 | Visio 绘制 | R0-R3，FSM 直接控制写入时机 |
| 图22 | 运算部分框图 | Visio 绘制 | latch_a → ALU(±) → latch_g 数据链 |
| 图23 | 控制电路框图 | Visio 绘制 | 5 状态 FSM + 每状态输出表 |
| 图24 | 芯片封装图 | Quartus Chip Planner | EP4CE55F23C8 |
| 图25 | 初始化复位波形 | ModelSim | Reset=1 清零, state=IDLE, 全 0x00 |
| 图26 | LOAD R0, 0x33 | ModelSim | **R0: 0x00→0x33**, 2 周期, 35ns 上升沿处变化 |
| 图27 | LOAD R1, 0x22 | ModelSim | **R1: 0x00→0x22**, R0=0x33 保持 |
| 图28 | LOAD R2, 0x11 | ModelSim | **R2: 0x00→0x11**, R0,R1 保持 |
| 图29 | ADD R0, R0, R1 | ModelSim | latch_a(0x33)→latch_g(0x55)→**R0=0x55**, 4 周期 |
| 图30 | MOVE R3, R0 | ModelSim | **R3: 0x00→0x55**, 2 周期, bus_sel=2 |
| 图31 | SUB R1, R1, R2 | ModelSim | **alu_sub=1**, latch_a(0x22)→latch_g(0x11)→**R1=0x11** |
| 图32 | 题目三 Flow Summary | Quartus | LE < 100, **Memory=0, Mult=0, PLL=0** |
| 图33 | 题目三 Fmax | Quartus TimeQuest | **Fmax > 200 MHz**, 远超 100 MHz |
